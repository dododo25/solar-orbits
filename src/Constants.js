const Second = 1;
const Hour = Second * 60 * 60;
const Day = Hour * 24;
const Month = Day * 27.3;
const Year = Day * 365.2425;

const OrbitSelectorBuffer = 8;
const BigOrbitSelectorBuffer = 16;

export {Second, Hour, Day, Month, Year, OrbitSelectorBuffer, BigOrbitSelectorBuffer};
